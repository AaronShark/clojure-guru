(ns clojure-noob.core
  (:gen-class))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))

(remove (set "aeiouy")
                   "vowels are useless! or maybe not...")